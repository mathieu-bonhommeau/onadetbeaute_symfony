{% extends 'pages/page.html.twig' %}

{% block title %}Tarifs{% endblock %}

{% block pageTitle %}Tarifs{% endblock %}

{% block pageContent %}

    <div class="tarifs">
       
        {% for key, prestationArray in prestations %}
        <div class="tarif-category card__title">{{ key }}</div>

        <div class="tarif-prestations">
            {% for prestation in prestationArray %}
            <div class="tarif-prestation">
                <div class="tarif-description">{{ prestation.name|raw }}</div>
                <div class="tarif-price"><span class="price">{{ (prestation.price * 0.01)|number_format(2)}}€</span></div>
            </div>
            {% endfor %}
        </div>

        {% endfor %}
        
    </div>
    
{% endblock %}